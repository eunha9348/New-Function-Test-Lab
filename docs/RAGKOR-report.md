# RAGKOR 분석 보고

> 웹 보고서: https://claude.ai/code/artifact/843a8edb-fd1d-413f-9b21-4c561e3f3ff7

## 한 줄 요약

한국어 문서에서 "모델이 말한 것이 원문에 실제로 있는가"를 판정하는 **규칙 기반 어휘 자원 + 자모 유사도 매처**.
표면형 10,000건 / 의미 클러스터 72개 / 시드 825개(사람 작성) / 사전 생성 0.05초 / 외부 의존성 없음.

**신경망을 훈련한 모델이 아닙니다.** GPU도 코퍼스 라이선스도 쓰지 않았고, 모든 항목이
어느 규칙에서 나왔는지 역추적됩니다(`origin`). 틀린 항목은 그 규칙만 고치면 되고 재학습이 없습니다.

## 왜 만들었나

기술제안서 PDF 실행 로그에서 세 줄이 이상했습니다.

```
[검증] 기계 검증기 8건 → 10건 → 13건 발견      ← 재작업할수록 늘어남
[감독] revise — 충실도 50점 → 30점 → 50점
소요 356.1초 · 추정 비용 $0.7653
```

감독이 환각으로 찍은 항목은 전부 원문에 실재했습니다. 감독이 지운 것은 환각이 아니라
**멀쩡한 성과 기술**이었습니다.

## 무엇이 문제였나

기존 검증기는 `정규화(인용) in 정규화(원문)` — 부분 문자열 포함만 봤습니다.
구두점 제거 목록에 `…`가 빠져 있었고, 모델이 긴 인용을 말줄임표로 자르는 순간 전부 환각이 됐습니다.

```
원문 · 연금 질의는 …다르다. 1. 틀린 답이 금전적 손해로 직결된다
인용 · 연금 질의는 …다르다. 1. 틀린 답이 금전적 손해로 직결된다…
기존 판정: 환각(blocker)   RAGKOR 판정: 근거 있음 · 일치도 97%
```

두 번째 문제: "근거 확보율 **22.8%p** 향상"을 환각으로 판정했습니다. 원문에 `32.9% → 55.7%`가
있으므로 22.8은 모델이 **옳게 계산한 값**입니다. 원문 문자열에 없다는 이유로 정확한 성과가 지워졌습니다.

이 오탐이 감독 프롬프트에 "기계 검증기 결과"로 들어가 재작업을 유발했고,
추출기는 지적을 피하려 멀쩡한 내용까지 빼면서 **매 라운드 결과가 나빠졌습니다.**

## 구조 (4층)

| 층 | 하는 일 |
|---|---|
| 자모 | 한글을 자모로 펴서 거리 측정. 두벌식 자판 인접키·모음 혼동쌍은 치환 비용을 깎는다.<br>개선/게선 0.95(오타) vs 개선/향상 0.47(다른 말) — 둘이 갈린다 |
| 정규화 | NFKC, 마크다운 표·글머리·페이지번호·`«불명»` 제거. 조사/어미 제거.<br>`18,799`·`18799`·`1만8799`를 같은 값으로 |
| 사전 | 시드 825 → 굴절·접사·오타 규칙으로 1만 건. 미수록 오타는 자모 유사도로 폴백.<br>커뮤니티·업무체 포함(갈아넣다, 삽질, 캐리했다, 빡셈) |
| 검증 | 인용을 4-gram으로 쪼개 **국소 밀도** 측정. 조각을 긁어모은 문장은 통과 못 함.<br>수치는 원문값/파생값/근거없음 3단 판정 |

## 사전 구성 (10,000건)

| 규칙 | 건수 | 내용 |
|---|---:|---|
| `typo` | 4,200 | 자판 인접·모음 혼동 오타 |
| `infl` | 4,200 | 굴절형 (서술성 명사에만) |
| `seed` | 510 | 사람이 쓴 의미 클러스터 멤버 |
| `affix` | 404 | 접사 조어 (효율→효율화·효율성·고효율) |
| `affix+infl` | 330 | 접사형의 굴절 |
| `variant` | 143 | 표기 흔들림 (컨텐츠→콘텐츠, 됬다→됐다) |
| `collo` | 111 | 신조어·커뮤니티·업무체 |
| `abbr` | 79 | 줄임말 (정처기, 팀플) |
| `affix-stem` | 23 | 접사 어간 |

우선순위 예산제로 생성하며 대표형 충돌은 빌드 때 검사합니다(현재 0건).

## 측정 결과

평가셋 180건은 실제 실패에서 역설계 — 감독이 환각으로 찍었던 인용의 변형 유형을
한국어 문서 3종에 적용하고 진짜 환각(창작·타 문서 문장·조각 긁어모음)을 섞었습니다.

| 지표 | 기존 | RAGKOR |
|---|---:|---:|
| 정밀도 | 1.000 | 1.000 |
| 재현율 | 0.848 | **1.000** |
| F1 | 0.918 | **1.000** |
| 오탐(정상→환각) | 21건 | **0건** |
| 미탐(환각 통과) | 0건 | 0건 |
| 말줄임표 인용 통과 | 2/23 | **23/23** |
| 오타 복원(사전 미수록) | — | **93.9%** (49건 중 46) |
| 유의어·신조어 인식 | — | **100%** (24쌍) |
| 수치 3단 판정 | — | **100%** |

## 파이프라인에 미친 영향

| 항목 | 기존 | 개선 |
|---|---:|---:|
| LLM 호출 | 8 | 6 |
| 상위 모델 입력 토큰 | 124,199 | **1,615** |
| 값싼 모델 입력 토큰 | 1,179 | 66,023 |
| 감독이 보는 원문 | 31,033자 | **2,746자** |
| 재작업 라운드 | 3 | **1** |
| 추정 비용 | $0.3144 | **$0.0691** (−78%) |

동일 문서(31,033자)로 전송량만 측정한 시뮬레이션(`tools/cost_sim.py`). 추론 구간만 비교하며 OCR은 양쪽 동일.

## 한계

- **의미가 아니라 표기를 봅니다.** 원문 문장을 그대로 가져와 맥락만 틀리게 붙인 경우는 통과합니다.
  벤치마크가 이 유형을 별도로 측정해 출력합니다(`[4] 알려진 미탐 유형`) — '한계' 섹션의 문장을
  '성과' 근거로 붙이면 `grounded (1.0)` 로 통과합니다. 의미 수준 검수는 감독 에이전트의 몫입니다.
- **사전은 도메인에 묶여 있습니다.** 시드는 경험 기록 도메인에 맞춰 썼습니다.
  다른 도메인은 시드 교체가 필요하지만, 재학습이 없어 교체 비용은 파일 하나입니다.
- **파생 수치는 단순 연산까지.** 차·합·비율·증감율만 확인하고 그 이상은 `unknown`으로 넘깁니다.
- **임계값은 이 평가셋 기준.** 0.85/0.55는 186건에서 고른 값이고 `ground.py` 한 곳에 있습니다.

## 남은 일

- 실제 문서로 전후 재측정 (지금 수치는 합성 변형 186건 기준)
- 섹션 제목 기준 맥락 검증 추가 → 남은 미탐 1건 유형 대응
- ~~TypeScript 포팅~~ — **완료**. `src/ragkor/` 에 같은 알고리즘으로 포팅했고,
  `src/validate/grounding.ts` 가 RAGKOR를 쓰도록 교체했습니다. 개별 점수(0.86/1.00)까지
  Python과 일치하는 것을 `test/ragkor.test.ts` 가 검증합니다.
- **파이프라인 개선도 TS에 반영 완료** — 사실 시트, 앙상블 배분 + 결정론적 병합, 충돌 중재,
  근거 구간 압축, 모델 세대 우선 선택, 품질 프리셋(`--quality fast|balanced|best`).

## 재현

```bash
python -m ragkor.build_lexicon   # 사전 생성
python -m ragkor.bench           # Python 벤치마크
python tools/cost_sim.py         # 비용 시뮬레이션
python colab/arc_colab.py        # Colab 테스트본 자체 점검 37종

npm test                         # TS 테스트 42종 (스키마 22 + RAGKOR 20)
python tools/gen_seed_ts.py      # 시드를 Python → TS 로 재생성
python tools/pack_colab.py       # ragkor 패키지를 Colab 단일 파일에 재주입
```
